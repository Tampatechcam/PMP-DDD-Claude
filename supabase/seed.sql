-- supabase/seed.sql
-- Local-only seed data. Real data lands via scripts/import-sheets.ts.
-- Keep this small and safe to run on a wiped local DB.

-- Example: one test client + one office. Remove or extend as needed.
-- insert into public.clients (name) values ('Test Client');
