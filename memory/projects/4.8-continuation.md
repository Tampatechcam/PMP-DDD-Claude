# PMP Dashboard — Claude 4.8 Continuation (single source of truth)

**Created:** 2026-06-13 · Updated 08:35 ET after Sonnet pushed full deploy track to origin/main.
**Continues from:** Sonnet handoff (`HANDOFF-2026-06-13.md`) + `standard-intake-pivot.md`
**Purpose:** One file containing ALL context. Pass this whole file to any new session and it can pick up cold. Cam pre-authorized "yes to everything" on the merge track.

---

## 0. TL;DR — where things stand RIGHT NOW

The standard-intake feature is **merged to `main`, pushed to GitHub, and typecheck-clean.** `origin/main` is at `bd50a0d`. Netlify auto-build should be running or done by now.

Critical: `be7e116`'s source was **truncated** by the sandbox Write tool — `package.json` plus 16 source files were chopped mid-statement. Subsequent commits on `main` repaired everything:

```
bd50a0d  fix(types): patch type errors in restored intake/audit files       ← origin/main, top
5d8272c  fix(intake): restore 16 source files truncated by sandbox Write
58f43e5  fix(deps): restore truncated package.json
28f6fa2  Merge origin/main into feature/standard-intake (no-content merge of PR #16)
5b61b7e  Merge pull request #16 from Tampatechcam/feature/bulk-delete-orders
be7e116  feat(intake): truncated source — only valid because of the three fixes above
3cbb1bf  feat(orders): bulk delete with row checkboxes on admin orders list
```

`npm run typecheck` exits 0 on `bd50a0d`.

---

## 1. Repo state

Repo: `C:\Users\cah50\Downloads\PMP DDD CLaude` · Deploy: **Netlify** (not Vercel) · DB: Supabase project `amtunktskgwvvqumrbde`

- `local main` = `bd50a0d` (synced with `origin/main`)
- `origin/main` = `bd50a0d` (deployed via Netlify on push)
- `feature/standard-intake` = `3a2e09b` (has docs commit + lockfile commit, parallel to main; not deployed)

The `feature/standard-intake` branch still has a 326-file CRLF-noise commit (`f117b9d`) that should NOT be merged. Main was rebuilt directly off the clean tree.

Working-tree state on disk: the 16 source files match `bd50a0d` exactly, plus a separate ~324 CRLF-only diffs against HEAD that should be discarded with `git checkout -- .`.

---

## 2. What `bd50a0d` contains (deployed)

### Standard intake template (replaces per-client CSV mappers)
- **Template:** `public/templates/standard-intake.csv` — downloadable, stable columns across all clients.
- **Schema:** `lib/intake/schema.ts` — Zod, ISO dates `YYYY-MM-DD`, 24-hour times.
- **Parser:** `lib/intake/parse.ts` — quoted fields, CRLF, escapes.
- **Server actions:** `lib/actions/intake.ts` — `previewIntakeCsv()` validates each row; `commitIntakeCsv()` resolves `client_name`/`office_name` to IDs, allocates next `order_number`, inserts in one shot, logs each row to `audit_log` with `source='admin-bulk-intake'`.
- **UI:** `/admin/intake` — 3 cards (download template, upload form, column reference). `IntakeForm.tsx` is the client island: file picker → validate → preview (valid/invalid) → commit.

### Audit log — write trail for every admin action
- **Table:** `public.audit_log` (id, table_name, row_id, action, source, actor_email, before_data, after_data, at). RLS: admin-only read.
- **Helper:** `lib/db/audit.ts` exports `recordAudit({...})`. Uses service-role admin client.
- **UI:** `/admin/audit` — filters (source, table, action, actor) + per-row JSON before/after diffs.
- **Sources in use:** `admin-bulk-intake`, `admin-status-edit`, `admin-delete`, `admin-bulk-delete`, `admin-invoice-void`, `client-proof-decision`.

### Venues normalized — 3-level cascade
- **New tables:** `public.buildings` (venue_id FK, name), `public.rooms` (building_id FK, name, capacity). `venues` gained `office_id`.
- **Back-populated:** 209 venues, 73 buildings, 92 rooms from "Venue • Building • Room" patterns.
- **Loader:** `lib/db/venue-cascade.ts:loadVenueCascade()` — single round-trip.
- **Form:** Office → Venue → Building → Room. Hidden inputs persist all IDs.

### Office defaults — pre-fill from history
- New office columns: `default_class_type`, `default_mailing_quantity`, `default_mailer_type`, `default_start_time`, `default_end_time`, `default_charity`, `default_needs_dm`, `default_needs_digital`, `default_needs_sheet`.
- Back-populated via mode aggregation.
- `AdminOrderForm` pre-fills once per office change. Default event count = 2 (Mon+Wed). `job_name` removed (server-generated).

### AI summaries server-side
- **Route:** `app/api/ai/summary/route.ts` — POST `{kind: 'upcoming' | 'deadlines' | 'revenue'}`.
- **Model:** `claude-haiku-4-5-20251001` via `@anthropic-ai/sdk`.
- **Requires `ANTHROPIC_API_KEY`** in env (route returns 500 without it — does NOT crash build).

### Recovery commits — what fixed the truncation

| Commit | What it fixed |
|---|---|
| `58f43e5` | `package.json` was truncated at byte 1491 (mid `@types/react-...`). Restored from `3cbb1bf` + added the two new deps. |
| `5d8272c` | 16 source files in `app/admin/audit`, `app/admin/intake`, `app/api/ai`, `components/admin`, `lib/actions`, `lib/db`, `lib/intake` were unclosed. Restored from `660c9f9` (the WIP stash merge that pre-dated the corruption). |
| `bd50a0d` | After restoration, typecheck surfaced bugs: `lib/supabase/admin.ts` was truncated mid-initializer; `AdminSidebar.tsx` had no `export function AdminSidebar`; Supabase select results needed explicit type casts in `intake.ts` / `audit-query.ts` / `offices.ts`; `parse.ts` needed `!` on `rows[0]` after the length guard. All patched. |

### Deps added
- `zod ^3.23.8`, `@anthropic-ai/sdk ^0.27.0`. Lockfile refreshed in commit `3a2e09b` on `feature/standard-intake`.

### Migrations applied (Supabase `amtunktskgwvvqumrbde`)
- `create_audit_log_table`
- `20260613000001_buildings_rooms_office_defaults.sql`

---

## 3. Files touched (high-level)

```
NEW
  public/templates/standard-intake.csv
  lib/intake/{schema,parse}.ts
  lib/actions/intake.ts
  lib/db/{audit-query,venue-cascade,audit}.ts
  app/admin/intake/{page,IntakeForm}.tsx
  app/admin/audit/page.tsx
  app/api/ai/summary/route.ts
  supabase/migrations/20260613000001_buildings_rooms_office_defaults.sql
  .claude/skills/will-warner-intake/SKILL.md
  memory/projects/standard-intake-pivot.md
  memory/projects/4.8-continuation.md   (this file)

MODIFIED
  components/admin/AdminOrderForm.tsx
  components/admin/SelectableOrdersTable.tsx
  components/layout/AdminSidebar.tsx
  lib/db/offices.ts
  lib/supabase/admin.ts                  (added createClient() factory)
  lib/actions/orders.ts                  (recordAudit on status/delete/bulk-delete)
  lib/actions/invoices.ts                (recordAudit on void)
  lib/actions/proofs.ts                  (recordAudit on client decision)
  app/admin/orders/new/page.tsx          (wire cascade + defaults)
  package.json                           (zod + @anthropic-ai/sdk)
```

---

## 4. Working-tree state right now

- `memory/projects/standard-intake-pivot.md` — on disk, not committed to main.
- `memory/projects/4.8-continuation.md` — this file, not committed.
- `HANDOFF-2026-06-13.md` — at repo root, not committed.
- `_claude-*.bat` and `_claude-*.log` — sandbox-driven deploy scripts and their output. Safe to delete.
- `.claude/skills/supabase-postgres-best-practices/` and `.claude/skills/supabase/` — untracked skill bundles.
- 324 "modified" files in `git diff HEAD` are CRLF-only flips. Discard with `git checkout -- .`.

---

## 5. STILL NEEDED — Cam's morning checklist

```powershell
cd "C:\Users\cah50\Downloads\PMP DDD CLaude"

# 1. Drop CRLF noise
git checkout -- .

# 2. Stage and commit the memory + handoff docs as one cleanup commit
git add memory/projects/standard-intake-pivot.md memory/projects/4.8-continuation.md HANDOFF-2026-06-13.md
git commit -m "docs(memory): standard-intake pivot + 4.8 continuation + handoff" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
git push origin main

# 3. (Optional cleanup) remove the deploy-driver bat scripts and their logs
del _claude-*.bat
del _claude-*.log
del _sentinel.txt 2>$null

# 4. Add ANTHROPIC_API_KEY to .env.local for local dev
# (Get the key from Anthropic console.)

# 5. Add ANTHROPIC_API_KEY to Netlify env vars
# Netlify dashboard -> Site settings -> Environment variables.
# Otherwise the /api/ai/summary route returns 500.

# 6. Verify Netlify build succeeded
# Netlify dashboard -> Deploys -> check the build for bd50a0d.
# If green, hit /admin/intake and /admin/audit on the live site.
```

### Netlify build status — UNVERIFIED locally
The sandbox couldn't drive `npm run build` reliably (.bat double-click wasn't executing on the Windows side), but `npm run typecheck` exits 0. TypeScript passing is a strong proxy for build success on Next.js 14. Netlify will validate end-to-end; check the Deploys tab.

---

## 6. Still open from Cam's earlier asks

- **Remove more "unused fields" from the order form** — candidates Cam named: sending list folder URL, QR code link, market, time notes. Exact list not confirmed — get Cam to confirm before editing `AdminOrderForm.tsx`.
- **Audit log retention** — none set; grows unbounded. Consider monthly partitioning in a future migration.
- **`feature/standard-intake` branch hygiene** — has `f117b9d` (326 CRLF files) and `3a2e09b` (lockfile) ahead of main. Either delete the branch or rebase off `bd50a0d`.

---

## 7. Cowork artifact role (changed)

`pmp-ops-command-center` artifact is now read-only / ops monitoring: order list, deadlines, AI summaries. All writes (status edits, deletes, intake) live in `/admin` so they get audit logging + the cascade UI.

---

## 8. How we got here (full history of the deploy track)

1. Cam asked to merge `feature/standard-intake` to main. `git stash pop` had left 7 files in conflict + index corruption (`index uses ��Po extension`).
2. Stripped conflict markers from the 7 files (kept the feature-branch / "Updated upstream" side).
3. Overwrote corrupt `.git/index` via `GIT_INDEX_FILE` plumbing (sandbox can't `unlink` on the Windows mount — only truncate/overwrite).
4. `git add -A` blocked by ghost `.git/index.lock` → worked around with `GIT_INDEX_FILE=/tmp/staging.idx git add -A`, then `git commit-tree` + `git update-ref`.
5. Discovered `be7e116` ALREADY held the entire feature → abandoned the new commit, fast-forwarded `main` to `be7e116`.
6. `git fetch` showed `origin/main` had moved to `5b61b7e` (parallel no-content bulk-delete PR merge). Created merge commit `28f6fa2` (parents `be7e116` + `5b61b7e`, tree = `be7e116`'s).
7. Pushed `28f6fa2` to origin/main via Windows-side `.bat` (cached GCM creds). Success.
8. `npm install` failed: package.json truncated at byte 1491. Reconstructed → commit `58f43e5` → pushed.
9. Typecheck failed: 16 source files truncated. Found complete versions in stash commit `660c9f9`. Restored all → commit `5d8272c` → pushed.
10. Typecheck showed real type errors. Fixed `lib/supabase/admin.ts` (truncated, added `createClient` factory), `AdminSidebar.tsx` (missing export), Supabase type casts in 4 files → commit `bd50a0d` → pushed.
11. Typecheck exits 0. Build verification via `.bat` couldn't execute on Windows side (double-click silently failed; Windows Terminal not in computer-use allowlist), so build is unverified locally — Netlify is the test.

---

## 9. Rules (from CLAUDE.md)

- Never `--no-verify`. Never disable RLS or `security_invoker` casually.
- Feature branches off `main`, PR back. `main` is the protected deploy target.
- Deploy = **Netlify**, not Vercel.
- Commits get `Co-Authored-By: Claude <noreply@anthropic.com>` trailer.
- One data layer: all DB reads through `lib/db/*.ts`, no raw `.from()` in pages/components.
- Three Supabase clients: `lib/supabase/{server,client,admin}.ts`. (admin.ts now exports both `supabaseAdmin` singleton AND `createClient()` factory.)
- Auth callback URL: `/callback` (NOT `/auth/callback`).

## Identity for commits

```
Name:  Cam Hebeler
Email: camhebeler@gmail.com
```
